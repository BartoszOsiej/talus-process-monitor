{
  "name": "talus-license-server",
  "version": "1.0.0",
  "private": true,
  "description": "License activation server for Talus Process Monitor (Cloudflare Workers + D1)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/*.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
