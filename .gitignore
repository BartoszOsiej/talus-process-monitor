# platform
.DS_Store
*.log
*.pdf

target/
.idea/
*.swp
*.swo
*.o

# Build outputs — compiled binaries and libraries are never committed
# (rebuild with: ./build.sh; releases attach them via CI)
build/
talus-web
talus-tui
process-monitor-tui
process-monitor-web
talus

# aurora-os is its own repo (BartoszOsiej/AURORA-OS)
aurora-os/

# ── Licensing secrets — NEVER commit these ────────────────────────────────
# Signing keys live outside the repo: ~/.secrets/talus/license-keys/
license-keys/
signing_key*
*.pem
.dev.vars
.env
.env.*
!.env.example
wrangler.local.toml

# license-server
node_modules/

# Wrangler local dev state
.wrangler/
pypi/dist/
